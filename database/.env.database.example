# Database Storage Configuration
# Add these to your .env file for task and ROI storage

# Default IDs for testing (replace with actual IDs in production)
DEFAULT_COMPANY_ID=default-company-id
DEFAULT_PROPERTY_ID=default-property-id

# Your existing Supabase configuration should already have:
# SUPABASE_URL=your-supabase-url
# SUPABASE_SERVICE_KEY=your-supabase-service-key